development:
  dialect: postgres
  datasource: host=localhost port=5432 dbname=postgres user=postgres password=postgres sslmode=disable
  dir: migrations
  table: migrations

test:
  dialect: postgres
  datasource: host=localhost port=5432 dbname=postgres user=postgres password=postgres sslmode=disable
  dir: migrations
  table: migrations
